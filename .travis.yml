osx_image: xcode8.2
before_install:
  - pip install --user awscli
  - export PATH=$PATH:$HOME/.local/bin
script: curl "https://api.sightr.io/uploadscript?branch=$TRAVIS_BRANCH&ci=travis&build=$TRAVIS_BUILD_NUMBER&repoKey=5957f3bc5979d932efb5ee8d&directory=screenshots&commit=$TRAVIS_COMMIT" | bash
